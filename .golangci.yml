version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - unused
    - govet
    - ineffassign
    - misspell
  settings:
    errcheck:
      exclude-functions:
        - io.Copy
  exclusions:
    presets:
      - std-error-handling
      - common-false-positives
    rules:
      - path: _test\.go
        linters:
          - errcheck
      - linters:
          - staticcheck
        text: "SA1019:.*viewport\\.(LineUp|LineDown|HalfViewUp|HalfViewDown)"

formatters:
  enable:
    - gofmt
    - goimports
